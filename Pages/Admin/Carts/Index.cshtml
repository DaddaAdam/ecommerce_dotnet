@page
@model ecommerce_dotnet.Pages.Admin.Carts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cart[0].UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cart[0].Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cart[0].CreatedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cart) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CartId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CartId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CartId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
